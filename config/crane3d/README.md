# 概要
 - このディレクトリは、機体関連の情報が入っています
 - 衝突判定と可視化で別々のデータを入力に処理を行っているためディレクトリが分かれています:
    1. machine_collision: 衝突判定, 機体除去用の情報が入ったディレクトリ
    2. visualize: open3dで表示する情報が入ったディレクトリ
 - open3dでの可視化は今後Godot UIに置き換わるので要らなくなりそうですが、READMEを作成した時点では必要なので、visualizeディレクトリは残しています
