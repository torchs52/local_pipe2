# CycloneDX Sunshine (vendored CLI)

Upstream: https://github.com/CycloneDX/Sunshine (Apache-2.0)

Pinned commit: see `UPSTREAM_COMMIT.txt`.

Used by `generate_dependency_licenses.py` to render `sbom-cyclonedx-1.6.json` as HTML.

```bash
pip install -r conan/vendor/sunshine/requirements.txt
```
